version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      - mongo
    command: python main.py

  mongo:
    image: mongo
    restart: on-failure
    volumes:
      - ./mongo_data:/data/database

volumes:
  mongo-data:
